# M1.1f — STL-wrap emits full U1 project structure (bl2u1-proven)

> STL-wrap path ONLY. Reference: bl2u1 (github.com/josuanbn/bl2u1) + orcasaved.3mf + u1_template.3mf. TDD, plan only.

## What bl2u1 actually does (reference findings)
- bl2u1 is a **3MF→3MF** converter. It uses `u1_template.3mf` **only** as the source of `project_settings.config`
  (it reads just that one part, app.py line 252-253). All *structure* comes from the user's input Bambu file, copied **verbatim**:
  `3D/3dmodel.model`, `3D/Objects/*`, `3D/_rels`, `model_settings.config`, `slice_info.config`, thumbnails.
- It rewrites exactly 3 parts: `project_settings.config` (U1 settings + filament arrays padded to **4**),
  `slice_info.config` (printer_model_id→Snapmaker U1, filament ids remapped, padded to 4),
  `model_settings.config` (extruder values remapped). Everything else copied.
- It NEVER builds a minimal 4-part 3MF and NEVER replaces geometry — it inherits a complete structure.
- `u1_template.3mf` itself is a **complete** U1 project: production extension (`requiredextensions="p"`, `p:UUID`
  on every object/component/build/item), mesh split into `3D/Objects/*.model` referenced by `<component>`,
  22 KB `model_settings.config` mapping every object→extruder, 4-filament `project_settings.config`,
  `slice_info.config` = 194 B stub, `[Content_Types]` with png+gcode, `_rels` with thumbnail relationships.

**Implication for STL:** an STL has none of this structure, so we must SYNTHESIZE the complete project that bl2u1
inherits for free. The canonical structure is identical across u1_template, orcasaved, and every real U1 file — and
is exactly what our `expG_object_mapping.3mf` already builds.

## Package inventory: bl2u1/stock/orcasaved (clean) vs our current STL-wrap
| part | u1_template | orcasaved | our STL-wrap (current) |
|---|---|---|---|
| `[Content_Types].xml` (png+gcode) | yes | yes | minimal (rels+model only) |
| `_rels/.rels` (+thumbnail rels) | yes | yes | model rel only |
| `3D/3dmodel.model` (component refs, UUIDs) | yes | yes | NO — embeds mesh directly, no UUIDs |
| `3D/Objects/*.model` (mesh) | yes | yes | MISSING (mesh inline) |
| `3D/_rels/3dmodel.model.rels` | yes | yes | MISSING |
| `Metadata/project_settings.config` | yes | yes | yes (== stock) |
| `Metadata/model_settings.config` (object->extruder) | yes | yes | MISSING <- prime suspect |
| `Metadata/slice_info.config` | yes | yes | MISSING |

## Minimum structural pieces every clean U1 project includes
1. `3D/3dmodel.model` — production extension; `<object id=N type="model">` wrapping `<components><component p:path="/3D/Objects/..." objectid=M p:UUID=.../></components>`; `<build p:UUID=...><item objectid=N p:UUID=... transform=... printable="1"/></build>`.
2. `3D/Objects/object_1.model` — the mesh as `<object id=M p:UUID=... type="model"><mesh>...`.
3. `3D/_rels/3dmodel.model.rels` — relationship root->object model.
4. `Metadata/model_settings.config` — `<object id=N><metadata key="name"/><metadata key="extruder" value="1"/><part id=1 subtype="normal_part">...<mesh_stat/></part></object>` + `<plate>...<model_instance><metadata key="object_id" value="N"/><metadata key="instance_id" value="0"/></model_instance></plate>` + `<assemble/>`.
5. `Metadata/slice_info.config` — 194 B stub is sufficient.
6. `Metadata/project_settings.config` — U1 settings (we already match stock).
7. `[Content_Types].xml`, `_rels/.rels`.

## Object / filament / extruder mapping (how bl2u1 wires it)
- `model_settings.config <object>.id` must equal the build-item `objectid` in `3dmodel.model` (bl2u1/orcasaved use the wrapper object id, e.g. 2).
- Each object/part carries `<metadata key="extruder" value="K">` (1-based filament index). For single-filament STL -> `1`.
- `project_settings.config` filament arrays: bl2u1 standardises to length **4** (pads dummy white PLA). We currently use length **1** (stock single-colour, also valid). KEEP 1 for now (simpler, matches Stock_U1_Blank); revisit only if Orca needs 4.

## Concrete patch plan for `stl_wrap` (NO code yet — pending expG Orca result)

**Task 1 — geometry emission split.** In `stl_wrap.py`, replace the single embedded-mesh `3dmodel.model` with:
- `build_object_model_xml(verts, tris)` -> `3D/Objects/object_1.model` (mesh, object id=1, `p:UUID`, production ns).
- `build_root_model_xml(transform)` -> `3D/3dmodel.model` (object id=2 -> component->object_1, build item objectid=2 + transform + `printable="1"`, `p:UUID`s, `requiredextensions="p"`).
- UUIDs via stdlib `uuid.uuid4()` (kernel stays pure; uuid is stdlib).

**Task 2 — `model_settings.config` generator.** `build_model_settings(object_id=2, name, extruder=1)` producing the `<object>`+`<part>`+`<plate><model_instance>` block above.

**Task 3 — `slice_info.config`.** Emit the 194 B stub constant.

**Task 4 — assemble in `wrap_stl_bytes`.** Parts = `[Content_Types].xml` (add png+gcode Defaults), `_rels/.rels` (model rel; thumbnails optional/skipped — no png), `3D/3dmodel.model`, `3D/Objects/object_1.model`, `3D/_rels/3dmodel.model.rels`, `Metadata/model_settings.config`, `Metadata/slice_info.config`, `Metadata/project_settings.config` (unchanged stock).

**Task 5 — tests.** Update `test_wrap_produces_valid_u1_3mf` for the new 8-part inventory; move geometry assertions to read `3D/Objects/object_1.model`; add `test_wrap_maps_object_to_extruder` (model_settings `<object id>` matches build-item objectid, extruder=1); keep `test_wrap_project_settings_equals_stock_blank`. `validate()` REQUIRED parts still satisfied.

**Task 6 — regenerate + manual Orca verify + commit.**

## Confidence
HIGH that this structure is correct (three independent references agree). The single open empirical question —
does adding the object->extruder mapping + split clear the dialog — is exactly what **expG_object_mapping.3mf**
tests. **Gate: confirm expG opens clean in Orca BEFORE implementing.** If expG is clean -> implement Tasks 1-6.
If expG still warns -> diff expG vs orcasaved for the residual delta (likely `[Content_Types]` png/gcode or `_rels`
thumbnail relationships) before coding.

## Guardrails
- STL-wrap only. 3MF repair / optimization / `profile.py` untouched.
- `project_settings.config` stays == stock (no preset/process/filament value changes).
- Geometry/placement preserved (mesh coords raw; bed-center transform on build item).
